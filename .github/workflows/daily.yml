name: Fund Sentinel (Daily Check)

on:
  schedule:
    - cron: '45 6 * * 1-5' # 06:45 UTC = 14:45 Beijing (Signal Check)
    - cron: '15 7 * * 1-5' # 07:15 UTC = 15:15 Beijing (Daily Report)
  workflow_dispatch:

# 🔥 核心变化1：赋予写入仓库的权限
permissions:
  contents: write

jobs:
  check_and_push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: pip install requests
          
      - name: Run Sentinel Script
        env:
          BARK_KEY: ${{ secrets.BARK_KEY }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
        run: python daily_check.py
        
      # 🔥 核心变化2：如果日记文件被修改了，把它提交回仓库
      - name: Commit and Push Log
        run: |
          git config --global user.name "FundBot"
          git config --global user.email "bot@github.com"
          git add signals.md
          # 只有当文件有变化时才提交，否则不报错
          git commit -m "📝 Update signal log" || echo "No changes to commit"
          git push
